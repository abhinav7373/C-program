//wap to print given no without last digit of the no//

#include<stdio.h>
#include<conio.h>
int main(){
    int a=1025;
    printf("the given no without last digit of the no is %d",a/10);
    return 0;
}