//wap to calculate area of rectangle(int from user) on the screen//

#include<stdio.h>
#include<conio.h>
int main(){
    int len,bre;
    printf("enter length and breadth\n");
    scanf("%d%d",&len,&bre);
    int res=len*bre;
    printf("%d",res);
    return 0;
}